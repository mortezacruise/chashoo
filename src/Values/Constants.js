import { Dimensions } from "react-native";

export const screenWidth = Dimensions.get("screen").width;
export const screenWidthH = Dimensions.get("screen").width / 2;
export const screenHeight = Dimensions.get("screen").height;
